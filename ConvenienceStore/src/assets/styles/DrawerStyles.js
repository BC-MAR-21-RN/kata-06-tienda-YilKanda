import {StyleSheet} from 'react-native';
import {KEPPEL, ZIRCON} from '../colors/Color';

const DrawerStyles = StyleSheet.create({
  background: {
    backgroundColor: KEPPEL,
    color: ZIRCON,
  },
});

export default DrawerStyles;
