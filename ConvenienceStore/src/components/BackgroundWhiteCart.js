import React from 'react';
import {View} from 'react-native';
import FormStyles from '../assets/styles/FormStyles';

const BackgroundWhiteCart = ({...rest}) => {
  return <View style={FormStyles.viewFrontgroundHome} />;
};

export default BackgroundWhiteCart;
