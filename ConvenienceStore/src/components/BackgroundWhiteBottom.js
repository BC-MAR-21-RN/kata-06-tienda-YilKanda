import React from 'react';
import {View} from 'react-native';
import FormStyles from '../assets/styles/FormStyles';

const BackgroundWhiteBottom = ({...rest}) => {
  return <View style={FormStyles.viewFrontgroundBottom} />;
};

export default BackgroundWhiteBottom;
